import {ConsoleErrorService} from './console-error-service';

describe('ConsoleErrorService', () => {
  it('should create an instance', () => {
    expect(new ConsoleErrorService()).toBeTruthy();
  });
});
